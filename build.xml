<?xml version="1.0" encoding="UTF-8"?>
<project name="Kraplow" default="migrate">
  <target name="migrate" description="Ant build is disabled. Use Maven.">
    <echo>====================================================</echo>
    <echo>This project now builds with Maven.</echo>
    <echo>Run: mvn clean package</echo>
    <echo>WAR: target/westerncardgame.war</echo>
    <echo>====================================================</echo>
    <fail message="Ant build disabled. Use Maven (mvn clean package)."/>
  </target>
</project>